[project]
name = "claude-md-discovery-extended"
version = "0.5.1"
description = "Flags CLAUDE.md files that never reached the context window, tracking what Claude Code actually loaded via the InstructionsLoaded hook"
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
